extends layout.pug

block content

  div(aria-live="polite")
    p(data-show-when="ready") Ready
    p(data-show-when="barred" hidden) Locked out!
    p(data-show-when="waiting" hidden) Hold on&hellip;
    p(data-show-when="open" hidden) Open :)

  div(class="pin" aria-live="polite")

  form
    button(type="button" data-key="49") 1
    button(type="button" data-key="50") 2
    button(type="button" data-key="51") 3
    br
    button(type="button" data-key="52") 4
    button(type="button" data-key="53") 5
    button(type="button" data-key="54") 6
    br
    button(type="button" data-key="55") 7
    button(type="button" data-key="56") 8
    button(type="button" data-key="57") 9
    br
    button(type="button" data-key="48") 0
    button(type="button" data-key="8" class="Delete") Delete